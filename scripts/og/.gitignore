# Intermediate build artefacts — generated by build-og.mjs.
# The final PNGs live in public/og/; these are ephemeral SVGs and HTML
# wrappers that exist only to feed headless Chrome's --screenshot.
og-*.svg
render-*.html
og.svg
render.html
