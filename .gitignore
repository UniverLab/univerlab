
# AI coding agents
.kiro/
.cursor/
.windsurf/
.claude/
.continue/
.copilot/
.kilocode/
.zencoder/
.qwen/
.agents/
skills-lock.json
# Created by https://www.toptal.com/developers/gitignore/api/astro
# Edit at https://www.toptal.com/developers/gitignore?templates=astro

### Astro ###
# Gitignore template for Astro projects
# Ignore content collection generated files
.astro

# End of https://www.toptal.com/developers/gitignore/api/astro

# Node
node_modules/
dist/
.astro/
npm-debug.log*

# Local env — never commit tokens
.env
.env.local
.env.*.local
*:Zone.Identifier

# og generator intermediates (source + font + og.png are committed)
scripts/og/og.svg
scripts/og/render.html

.mimocode/
specs/

# Cloudflare Workers local state
.wrangler/

# Local-only ops scripts for the get Worker (query helpers, etc). They need
# credentials to run, so they are deliberately not part of this repo.
workers/get/scripts/
.dev.vars
.auth-token.local

# Test coverage reports
coverage/
